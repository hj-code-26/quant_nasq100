__pycache__/
*.py[cod]
.venv/
venv/
.DS_Store

# 비밀키
.env

# 매매 기록 DB
trading_decisions.db

# 백테스트 일봉 캐시
data_cache/

# 토스 토큰 캐시 (프로세스 간 공유)
.toss_token.json

# 학습 모델 캐시
models_cache/

# 런타임 로그·백테스트 출력
server.log
bt_*.txt
bt_*.err

# 백엔드 런타임 기록
backend/
# 실행 로그 (컴퓨터마다 따로 쌓이므로 추적하지 않는다)
autotrade.log
.env.bak*

# 실계좌 진입일 프로브 산출물 (계좌 보유·주문 내역 — 추적하지 않는다)
research/out/entry_probe_*
