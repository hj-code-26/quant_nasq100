# 감사 보고서 — 토스증권 나스닥100 자동매매

재검증 프롬프트(`claude_us_equity_revalidation_prompt.md`)에 따른 감사·수정 기록.
브랜치 `audit/revalidation-p0`. **실주문 없음, 운영 .env·DB 변경 없음.**

재현:
```bash
python test_validate.py && python test_indicators.py && python test_session.py
python backtest.py --daily
```

---

## 0. 결론 먼저

**배포 보류.** P0(주문·리스크) 결함은 고쳤고 회귀 테스트가 붙었지만, P1·P2 의 핵심 —
point-in-time 유니버스, 과거 LLM 재현, 비용 포함 성과의 불확실성 — 은 **데이터가 없어
해소되지 않았다.** 즉 "성능이 개선됐다"고 말할 근거는 이 저장소 안에 아직 없다.
지금 상태로 할 수 있는 정직한 주장은 **"실행 안전성이 개선됐다"** 까지다.

---

## 1. 감사표

구분: **[결함]** 코드에서 확인된 실제 결함 · **[문서]** 문서와 구현의 불일치 ·
**[반례]** 합성 반례로 재현한 것 · **[가설]** 미검증 가설(고치지 않았거나 근거 없음)

| # | 구분 | 내용 | 상태 |
|---|---|---|---|
| P0-1 | 결함 | `clientOrderId = f"at{run_id}-..."`. `run_id` 는 **머신마다 다른 로컬 DB 시퀀스**라 다른 PC·다른 프로세스가 같은 매수 의도를 내면 서로 다른 id → 중복 주문이 그대로 나간다 | 고침 |
| P0-2 | 결함 | 미체결 주문이 있으면 **모든** 매도를 스킵. 손절·만기·노출 축소가 낡은 매수 주문 하나 때문에 무기한 지연 | 고침 |
| P0-3 | 결함 | 위험 관리가 진입 분석 **뒤**에 있었다. `decide_all` 이 비면 `RuntimeError` 로 사이클이 끝나 손절·만기가 아예 안 나감 | 고침 |
| P0-4 | 결함 | 매도를 **제출**하면 `positions.discard()` 로 슬롯이 비었다. 체결 전에 자리를 재사용 → 최대 종목 수 초과 가능 | 고침 |
| P0-5 | 문서 | 대시보드(`autotrade_server.py`)를 끄면 스케줄러가 죽어 보호 로직까지 멈춘다 | 미조치(§4) |
| P0-6 | 결함 | 게이트웨이 경유 시 서버측 json_schema 강제가 없는데 검증은 "필수 키 존재" 뿐. 임의 필드·문자열 숫자·NaN/Inf·범위 밖 비율이 통과 | 고침 |
| P0-7 | 가설 | 토큰 파일은 원자적 교체(`tmp.replace`)로 **같은 머신** 경합만 막는다. 토스는 신규 발급 시 이전 토큰을 무효화하므로 **다중 PC 는 여전히 서로 죽인다** | 미조치(§4) |
| P1-1 | 결함 | `trading_days_since` 가 주말만 제외 → 공휴일마다 20거래일 만기가 하루씩 **일찍** 왔다 | 고침 |
| P1-2 | 결함/반례 | 변동성 타겟 `cap = target/account_vol`. 계좌 변동성엔 이미 현금이 섞여 있어 되먹임이 뒤집힌다 | 고침 |
| P1-3 | 결함 | `daily_sim` 이 t일 종가 신호를 **같은 t일 종가**에 체결 (하루 선행) | 고침 |
| P1-4 | 결함 | equity 이력이 총자산 한 칸뿐 → 입출금·환전이 그대로 '수익률'로 잡혀 변동성 추정을 오염 | 부분 조치 |
| P1-5 | 문서 | `backtest.py --daily` 기본 설정(5슬롯·30%·손절15·모멘텀청산)이 운영 설정(10슬롯·15%·만기20·손절 없음)과 달랐다 | 고침 |
| P1-6 | 가설 | 유니버스가 **오늘의** 나스닥100 → 생존 편향. `market_data.py` 는 주석으로 인정, 실제 보정은 없음 | 미해소(§3) |
| P1-7 | 가설 | 실전은 "큰 종목부터 축소", 백테스트는 "비례 축소". 동일 총노출 ≠ 동일 위험 | 미해소(§3) |
| P0-8 | 결함 | `bear_derisk` 가 지수 일봉이 없거나 253봉 미만일 때도 `None` 을 돌려줘 **판정 불가**와 **신호 OFF** 가 구분되지 않았다. 로그는 "신호 미점등"이라고 적어 하락장에 보호가 조용히 사라져도 알 수 없다 (오버레이 ON 일 때만 영향 — 기본값은 OFF) | 고침 |
| P1-8 | 가설 | QQQ 실패 시 유니버스 중앙값으로 자동 전환 = **다른 전략**으로 갈아타는 것. 성적이 더 낮다고 주석에 적혀 있으면서 자동 전환한다 | 미조치(§4) |

---

## 2. 수정 내역

### P0-1 주문 의도 키 · 대사
`intent_key(o)` = `at{YYMMDD}{HH}{MM/30}{종목}{B|S}` (미국 시각 30분 슬롯).
**의도 자체**로 만들어 어느 PC/프로세스에서 내도 같은 id → 브로커가 중복을 거절한다.
`place_all` 은 제출 **전에** DB 에 `status='pending'` 으로 의도를 남기고(durable intent),
`TossError` 가 나면 `find_order(coid)` 로 실제 접수 여부를 대사한 뒤에만 실패 처리한다.

> **과장 금지**: 30분 버킷은 스케줄이 대략 같은 시각에 도는 것을 전제한 근사고,
> 두 프로세스가 **정확히 동시에** 제출하는 경합은 못 막는다. 그건 브로커의 clientOrderId
> 중복 거절에 의존한다. 단일 머신 락으로 다른 PC 까지 막았다는 주장은 하지 않는다.

### P0-2 취소 → 대사 → 매도가능 수량 → 축소
코드가 강제하는 매도에 `forced=True` 를 붙이고, 미체결이 있으면 스킵 대신
`cancel_first=True` 로 내보낸다. 체결 직전 `free_position()` 이
**취소 요청 → OPEN 소멸 대사(최대 20초) → `sellable-quantity` 재조회 → 수량 clamp** 순으로 처리한다.
취소가 거절되면(=이미 체결) 판단은 항상 재조회 결과로 한다.
Claude 의 **재량** 매도는 예전대로 미룬다 — 경합을 만들 이유가 없다.

### P0-3 위험 관리 루프 분리
`validate_orders(plan={"orders":[]}, decisions={}, ...)` 로 부르면 LLM 없이 손절·만기·노출
축소만 나온다. `run_cycle` 이 이 패스를 **스크리닝보다 먼저** 돌려 즉시 주문하고,
계좌를 다시 읽은 뒤 진입 분석으로 넘어간다. LLM 이 전부 실패해도 청산은 나간다.
`skip_symbols` 로 같은 사이클에서 두 번 건드리지 않는다.

추가: `STALE_MAX_MIN`(기본 20분). 스크리닝~배분이 이보다 오래 걸리면 시세가 낡은 것으로
보고 **신규 매수만** 취소한다(매도는 계속). 배분 직후 계좌를 재조회한다.

### P0-4 슬롯·현금
`positions.discard()` 제거. 매도 제출로는 자리가 나지 않는다.
매도 대금도 `cash_left` 에 더하지 않는다(원래대로 보수적). 위험관리 패스 뒤 계좌 재조회로
**실제 체결된 만큼만** 반영된다.

### P0-6 strict schema
`check_schema()` — type/required/`additionalProperties:false`/enum/minimum/maximum/정수/NaN·Inf.
게이트웨이 경로의 `raw_decode` 결과에도 동일 적용. 테스트로 임의 종목·문자열 숫자·NaN·
범위 밖 비율·enum 밖 side·승인 문자열 필드를 전부 거부하는 것을 확인.
포지션 없는 매도·중복 주문은 `validate_orders` 에서 거른다(중복 검사 신규 추가).

### P1-1 실제 거래소 달력
`TRADING_DAYS` 를 지수(QQQ) 일봉 인덱스에서 채운다 — 공휴일·조기폐장이 그대로 반영된다.
캔들을 못 받으면 주말 근사로 떨어지고 **경고를 남긴다**. 같은 캔들을 국면 판정과 공유해
API 호출은 늘지 않는다.
lot age 정책 명시: 최초 진입 기준, 추가 매수는 시계를 리셋하지 않고, 부분 매도는 나이를
유지하며, 전량 청산 후 재진입은 새로 시작(`position_entry_map` 의 0→양수 전환 시점).

### P1-2 변동성 타겟 — 합성 반례로 확인
`cap = target / (account_vol / 평균 주식비중)`. 계좌 변동성을 같은 창의 평균 주식 비중으로
나눠 **주식 부분의 변동성**을 복원한 뒤 목표와 비교한다.

프롬프트가 요구한 60% 변동성 자산 / 목표 30% 반례 (`test_validate.py` 마지막 블록):

| | 주식비중 50%, 계좌 변동성 30% |
|---|---|
| 옛 방식 | `30/30` = **상한 100%** → 다 사라 → 계좌 60% → 상한 50% → 다시 100% … 진동 |
| 새 방식 | `30/(30/0.5)` = **상한 50%** → 목표 비중에서 고정 |

테스트 실행 로그: `계좌 실현 30.3% ÷ 평균 주식비중 50% = 주식 60.5% / 목표 30% → 상한 50%`

> 한계: 계좌 이력만으로는 Σ 를 복원할 수 없어 **단일 자산 근사**다. 목표 포트폴리오 w 의
> `sqrt(w'Σw)` 기반 방식은 구현하지 않았다(종목 공분산 이력이 필요 — §3).

### P1-3 체결 시점
`daily_sim`: 신호 = t-1 종가, 체결 = t 시가. `opens=None` 이면 옛 방식이 남아 A/B 비교가 된다.

### P1-4 입출금 분리 (부분)
`equity` 에 `stock_value`·`cashflow` 컬럼 추가. 수익률은 `(V_t − CF_t)/V_{t-1} − 1`.
**토스 Open API 에 입출금 조회가 없어 `cashflow` 는 코드가 채우지 못한다** — 수동 기입이
필요하고, 하루 ±25% 이상 점프가 보이면 경고를 남긴다.
달러 전략수익 / 원화 환산수익 분리와 일중 현금흐름 TWR 은 **구현하지 않았다**(§3).

### P0-8 판정 불가 ≠ 신호 OFF
`bear_data_ok(df)` = `df is not None and len(df) >= 253` 를 분리하고 `run_cycle` 이
판정 불가일 때 `log.error` 로 **"평가하지 못했다, 축소 없이 진행한다"** 를 남긴다.
동작(=개입하지 않음)은 바꾸지 않았다 — 데이터 없이 축소 매도를 내는 fail-closed 는
정책 변경이라 승인 대상이다(§4). 회귀: `test_validate.py` 253봉 경계.

### P1-5 백테스트 파라미터 동등성
`--daily` 표에 운영 설정 행 `⓪ 만기20·10슬롯·15%` 추가, `hold_days`(만기 청산) 구현.

---

## 3. 해소하지 못한 것 (데이터·재현 불가)

프롬프트가 요구한 P2 A~H 중 **아래는 실행하지 않았다.** 근거 없이 수치를 만들지 않기 위해서다.

1. **point-in-time 유니버스 없음.** 당시 구성종목·편입/편출 시각·상장폐지·합병·티커 변경이
   없다. 현재 나스닥100 을 과거 전체에 적용한 모든 결과는 **탐색 전용**이며,
   **생존 편향을 제거하지 못했다.** 절대 수익률뿐 아니라 규칙 간 상대 비교도 왜곡된다
   (하락장에서 특히). → C·F·G 의 결론을 이 데이터로 확정할 수 없다.
2. **과거 LLM 재현 불가.** 역사적 뉴스 스냅샷·프롬프트·응답·모델 버전 기록이 없다.
   현행 모델은 과거 결과를 이미 학습했을 수 있으므로 **익명화만으로 누수가 제거됐다고
   말할 수 없다.** → D(LLM 추가가치)는 forward shadow 로만 측정 가능하며, 코드 동결 후
   실시간 기록이 쌓여야 한다.
3. **2015~2026 은 최종 OOS 가 아니다.** 반복 선택에 쓴 검증셋이다. nested walk-forward,
   겹치는 20일 라벨의 purge/embargo, 전체 시도 수 기록 — 모두 미구현.
4. **block bootstrap 미구현.** 겹치는 개별 거래를 독립 표본으로 부트스트랩하면 안 되고,
   일별 포트폴리오 수익률의 시계열 block bootstrap 이 필요하다.
5. 종목별 실제 스프레드·시장충격·분할체결·배당·현금이자·환전 비용, 섹터/상관군 한도(F),
   국면 경계 hysteresis(G) — 미측정. 특히 **-4% 진입 / -2% 해제 같은 값은 미검증 후보**다.

### 실행한 유일한 비교 — B(엔진만 수정)
`backtest.py --daily`, 토스 3년 일봉, **오늘의 유니버스(생존 편향 있음)**, 편도 0.25%:

| 규칙 | A 총수익 (같은 종가 체결) | B (다음 시가 체결) | Δ |
|---|---|---|---|
| ⓪ 운영 설정 (만기20·10슬롯·15%) | +145.4% (Sharpe 1.14) | +159.9% (1.23) | +14.4%p |
| ① 손절15+모멘텀0 | +156.4% (1.00) | +168.2% (1.06) | +11.7%p |
| ④ 손절25+모멘텀0+회전가드 | +214.3% (1.19) | +261.5% (1.29) | +47.1%p |

**읽는 법**: 하루 선행 편향을 제거했더니 성적이 **떨어지지 않고 올랐다.** 즉 기존 우위가
같은-종가 체결에서 나온 것은 아니다. 다만 이것을 "개선"으로 읽으면 안 된다 —
표본은 3년·생존 편향 유니버스이고, 갭 상승이 모멘텀에 유리했던 특정 구간의 성질일 수 있다.
검증 구간이 약 1년(2025-08~2026-09)뿐이라 그 구간의 CAGR(+100% 대)은 해석하지 말 것.

**서로 다른 표의 수치를 하나의 최종 포트폴리오 성적으로 합치지 않았다.**

---

## 4. 사용자 승인이 필요한 운영 변경 (미조치)

코드로 바꾸지 않았다. 승인 후 별도 작업.

- **P0-5 보호 로직 상시 실행**: 지금은 대시보드 프로세스가 스케줄러를 물고 있어, 창을 닫으면
  손절·만기도 멈춘다. `autotrade.py` 를 서비스/작업 스케줄러로 분리하고 대시보드는 조회
  전용으로 두는 구조를 권한다. 감시·자동 재시작·하트비트 경보 포함.
- **P0-7 다중 PC 토큰 정책**: 파일 공유는 같은 머신에서만 유효하다. 다중 PC 를 쓸 거면
  (a) 봇은 한 대만 돌리거나 (b) 토스 공식 명세에서 다중 토큰 허용 여부를 확인해야 한다.
  명세 확인 없이 구현하지 않았다.
- **P1-8 국면 지표 폴백**: QQQ 실패 시 유니버스 중앙값으로 자동 전환하는 것은 조용히 다른
  전략으로 갈아타는 것이다. 성적이 더 낮다고 주석에 적혀 있다. 대안 — 폴백 대신
  **신규 진입만 보류하고 위험 축소는 유지**, 실패/회복을 로그로 남기기.

---

## 5. 다음에 해야 할 순서

1. point-in-time 유니버스 확보 (없으면 C·F·G 결론은 확정 불가로 남긴다)
2. 코드 동결 → forward shadow 기록 (LLM 승인/거부·수익·회전율·지연·API 비용)
3. block bootstrap + nested walk-forward 로 차이의 불확실성 산출
4. 그 결과가 비용 포함으로 유의할 때에만 배포 재검토
