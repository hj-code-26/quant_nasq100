# 결론 — 네 상태를 분리해서 본다

commit `2417a17`, 브랜치 `audit/offline-safety-revalidation`. 운영 코드 수정 0줄.
근거는 `research/out/`, 재현은 `research/run_commands.txt`.
**배포 보류 유지. 연구 폴더 격리 유지. 새 전략 후보·파라미터 탐색 없음.**

네 가지는 서로 다른 주장이고 근거도 다르다. 하나가 통과했다고 다른 것이 통과하지 않는다.

| 상태 | 뜻 | 판정 | 근거 |
|---|---|---|---|
| **① 연구 엔진 정확성** | 엔진이 시간을 지키고 회계가 맞는가 | **통과** | 항등식 오차 <1e-9, 불변식 2건, 결함주입 6건, 손계산 4건 |
| **② 모의 주문 안전성** | 모의 환경에서 위험 경로가 지켜지는가 | **통과** | 실제 함수 대상 22+9+5건, 주입 시 실패 확인 |
| **③ 실전 동등성** | 연구 엔진이 실전과 같은가 | **미입증** | 모드 C 는 하루 1회 개장 시장가 가정. 실브로커·다중PC·다회 사이클 미검증 |
| **④ 전략 성능** | 우위가 있는가 | **부정적** | PIT(2015~) 적용 시 검증 구간 CAGR 26.9%→12.5%, **QQQ 매수보유(18.9%)에 진다** → `pit_conclusion.md` |

---

## ① 연구 엔진 정확성 — 통과

- **재현성**: 계측 엔진 A·B 가 기존 `backtest.daily_sim` 과 자산곡선 완전 일치(diff 0).
  ⓪ +145.4%→+159.9%, ④ +214.3%→+261.5% 재현. → `ab_summary.csv`, `manifest.json`
- **회계**: 9개 조합 전부 `초기현금−매수+매도+미청산평가 = NAV`, 오차 <1e-9.
  → `accounting_identity.csv`
- **available_at 모델** (일괄 shift 아님): 입력별 가용 시각을 `engine.py::AVAILABILITY` 에
  명시하고 **주문 종류별로** 다르게 처리한다.
  - 금액 주문(정규장 소수점): 달러 금액만 t-1 정보로 정하고 수량은 체결가로 결정 → 누수 없음
  - **수량 주문(정수 주, 장외): 수량을 t-1 종가로 산정**한다. t 시가로 정수 수량을 정하면
    미래 정보다 — 이번에 고쳤다(N4). 손계산 fixture 로 9주를 직접 확인.
  - 체결가는 t 개장(사후), NAV 마크는 t 종가(회계 전용, 결정 금지)
- **당일 종가 불변식**(prefix-invariance 와 별개 테스트): t 일 종가만 ±25~30% 바꿔도
  t 일 시가 주문이 불변. 주문이 실제로 난 날만 골라 검사한다(빈 날 비교는 무력).
- **결함 주입 6건 — 되살리면 테스트가 실제로 실패한다**:
  누수 복원(모드 B) → 불변식 깨짐(2024-04-19 KHC 매수 수량이 당일 종가에 반응),
  슬롯 재사용, 부분체결 clamp 제거, `intent_key` 프로세스 고유값, stale 필터 제거,
  현금 유지 제거. → `test_invariance.py`, `test_results.txt`

## ② 모의 주문 안전성 — 통과 (모의 한정)

실제 함수 대상 36건. 입출금 제거·DST·부분청산 나이·취소 경합·부분체결 clamp·
타임아웃 후 대사·durable intent·매도 대금 재사용 금지·노출 상한의 의미·golden replay·
프롬프트 주입 내성·시간 순서 불변식. → `test_results.txt`

**운영 격리 (mtime 을 넘어선 강제)** — `research/isolation.py`:
자격증명 환경변수 제거 · `.env`/토큰 파일 읽기 차단(빈 내용 대체, 값은 프로세스에 안 들어옴) ·
운영 DB(+WAL/SHM)·토큰·`data_cache`·운영 소스 **쓰기 차단** · 토스 호스트 HTTP 차단 ·
운영 로그 리다이렉트 · 145개 파일 지문(크기·mtime·sha256) 검증.
최종 검증: `{'changed': [], 'ok': True}` → `manifest.json` `isolation_verify`

> **1차 보고 정정 (N3)**: 1차의 "운영 미접촉"은 **mtime 만** 근거였고 불완전했다.
> 격리 하네스를 넣자 두 가지가 드러났다 — 연구 스크립트가 `autotrade` 를 import 하며
> **운영 `autotrade.log` 에 append** 했고, `exposure_cap` 이 **운영 DB 를 쓰기 모드로 열었다**.
> 지금은 둘 다 차단·리다이렉트된다. (저장소 자체 테스트 `test_validate.py` 등은 원래대로
> 운영 로그를 쓴다 — 연구 코드가 아니라 기존 동작이다.)

## ③ 실전 동등성 — 미입증

모드 C 는 **연구 기준 엔진**이다. 하루 1회 개장 시장가 체결을 가정한다.
운영은 하루 여러 번 사이클을 돌고 프리/애프터장 지정가와 LLM 재량이 들어간다.
**모드 C 가 실전 다회 실행과 동등하다고 말하지 않는다.** → `engine.py` 주석, `manifest.json`

미검증으로 남은 것: 실브로커 타이밍, 다중 PC split-brain, 실시장 장애, buying power API,
`run_cycle` 통합 경로(낡은 스냅샷 처리는 인라인이라 소스 수준 점검까지만).

## ④ 전략 성능 — 미확정 → **부정적** (2026-09-12 갱신)

> PIT 구성종목(2015~)을 넣고 다시 재니 검증 구간에서 **QQQ 매수보유를 못 이긴다**
> (CAGR 12.53% vs 18.94%, Sharpe 0.569 vs 0.901, ΔSharpe −0.416 CI −0.692~−0.164).
> 아래 A/B·C1/C2/C3 수치는 전부 **편향 있는 유니버스**에서 나온 것이다 — `PIT=0` 상태의
> 기록으로 남겨 두되, 성능 판단의 근거로 쓰지 않는다. 상세: `research/pit_conclusion.md`
>
> **2026-09-15 G**: PIT 에서 청산·국면·진입 37개 설정을 사전등록 기준으로 재측정. 채택(DSR≥0.95) 0개.
> 가격 기반 매도(손절·모멘텀 음전·50일선)는 여전히 무효, 섀도 후보는 126일 모멘텀 + 만기 연장(P1).
> 운영 근사 L0 는 QQQ 에 유의하게 진다 (ΔSh −0.448, CI −0.834~−0.098). → `research/g_conclusion.md`
> **G-3**: 과거 자료로만 고르면 '매년 1위 추종'은 실패, 1999~2014 로 고른 **만기 40일**만 2015~2026
> 표본 외에서 L0 를 이긴다 (ΔSh +0.243, CI +0.029~+0.468; 25bp +0.402). 유의한 α 는 어디에도 없다.

### A/B 차이의 완전 분해 (반올림 전 합계 검증, 오차 ~1e-12)

두 층 모두 정확히 합이 맞는다. → `ab_attribution.md`, `ab_attribution.csv`

⓪ B−A = **+1,442.37 USD (+14.42%p)**:

| 갈래 | USD |
|---|---|
| 시프트-가격/체결 (같은 종목이 하루 밀림) | −1,101.22 |
| 시프트-사이징 (같은 종목, 수량 차이) | +40.12 |
| 시기 변경 (같은 종목, 다른 시기) | **+2,737.77** |
| **구성종목 변경 (실제 종목 교체)** | **−234.30** |

④ B−A = **+4,711.44 USD (+47.11%p)**: 시프트-사이징 +3,617.57, 시프트-가격 +2,288.95,
시기 변경 −1,119.16, **구성종목 변경 −75.91**.

> **1차 보고 정정**: 1차에서 잔여분을 "다른 종목을 들게 된 효과"라고 썼는데 **틀렸다.**
> 실제 구성종목 변경은 ⓪ −234, ④ −76 USD 로 거의 0 이다. 실제 동인은
> **같은 종목의 재진입 시기 변화(⓪)** 와 **같은 종목의 수량 차이(④)** 다.
> 짝짓기 기준은 사전 정의(같은 종목·진입일 ≤3 거래일). 체결 불가는 full-path 에는 없고
> frozen-intent 진단의 2건뿐이며 표본 마지막 날이라 다음 세션이 없어서다.

### 통계 (신뢰수준 95%, percentile CI, 재표집 2000회, seed 20260910)

| 비교 | 단위 | 점추정 | 95% CI | 양수 재표집 비율 | CI 0 제외 |
|---|---|---|---|---|---|
| ⓪ B−A | 누적수익 배수 차 | +0.1442 | [−0.465, +1.210] | 0.689 | 아니오 |
| ④ B−A | 누적수익 배수 차 | +0.4711 | [−3.346, +8.966] | 0.623 | 아니오 |

> `frac_resamples_gt0` 는 **재표집 결과 중 차이가 양수인 비율**이다. 진짜 우위의 확률도,
> 실전 성공 확률도, p-value 도 아니다. **CI 가 0 을 포함한다는 것은 "이 표본으로는 부호를
> 가릴 수 없다"는 뜻이지 동등성·무효과의 증거가 아니다** — 동등성을 주장하려면 사전에 정한
> 동등성 마진과 TOST 같은 절차가 따로 필요하고, 이번에 하지 않았다. → `bootstrap_summary.csv`

### 국면지표 출처 (모든 실험 추적) → `regime_source.csv`

**⓪·①·④·BASE·C1·C2 는 국면 판정을 아예 쓰지 않는다** (cfg 에 `regime` 키가 없다).
QQQ 부재의 영향을 받은 실험은 **C3 뿐**이었다. 따라서 "운영설정 성과를 폴백 기반 proxy 로
재표시"할 필요는 없다 — 애초에 폴백을 타지 않았다.

C3 는 QQQ 를 확보해 **운영 기본 지표로 재실행**했고 폴백과 나란히 냈다:

| 지표 | 단일경계 −3% | 완충 −4%/−2% | 전환 횟수 |
|---|---|---|---|
| QQQ (운영 기본) | +33.41% | +39.52% | 24 → 16 |
| 유니버스 중앙값 (폴백) | +38.29% | +38.25% | 18 → 8 |

두 지표 모두 완충−단일경계 차이의 CI 가 0 을 포함한다. **지표를 바꾸면 하락 판정일이
57일 → 104일로 거의 두 배**가 된다 — 폴백은 같은 전략이 아니다.

### C1 은 사전등록 탈락 조건에 걸렸다 → `c1_vs_cashmix.csv`

| | 총수익 | 변동성 | MDD | ES5% | 포기수익 ÷ 줄인변동성 |
|---|---|---|---|---|---|
| 기준선 | +155.74% | 29.56% | −31.31% | −4.17% | — |
| C1 위험타겟30% | +102.10% | 26.77% | −23.99% | −3.80% | **19.22** |
| 단순 현금혼합 75% (사전 지정) | +149.70% | 23.77% | −24.59% | −3.15% | **1.04** |
| 단순 현금혼합 76% (사후 진단) | +168.41% | 25.57% | −24.57% | −3.38% | (수익 증가) |

**C1 은 단순 비중 축소보다 나쁘다.** 변동성 2.79%p 를 줄이려고 수익 53.6%p 를 포기했고,
같은 낙폭 감소를 단순 현금혼합은 6.0%p 로 얻었다. → **C1 탈락.**
'76% 맞춤' 행은 전체 표본의 사후 실현 노출로 배율을 맞춘 **사후 진단**이며 실행 불가능하다.

비례 축소 vs 큰 순 축소: 실현변동성 26.77% vs 26.68%, 하방편차 18.83% vs 18.51%,
MDD −23.99% vs −23.81%, ES5% −3.80% vs −3.72%. 차이의 CI 는 0 을 포함한다.

C2: 만기 당일 동일 종목 재매수 **0건**(만기 청산 315건 중) → 비용 절감 주장 취소.
회전율 105 → 76 으로 줄지만 수익도 −58.5%p, CI 는 0 을 포함한다.

---

## 데이터·병렬 진행

| | 상태 |
|---|---|
| QQQ 국면지표 | **확보 완료** (무료·비상업, 유료 구매·브로커 API·신규 토큰 없음) |
| PIT 유니버스 | 미확보 — 스키마·검증 규칙만 정의 |
| forward shadow | **기록 기반 준비 완료** (스키마 + replay/주입내성 5건 통과, 완결 코호트 0) |

shadow 는 PIT 를 기다리지 않는다. 파라미터 동결 조건(`shadow-2026-09-freeze`)과
사전 평가 기준(완결 코호트 ≥3, 관측 ≥60거래일)이 `shadow.db` 에 등록돼 있고,
운영 판단 252행을 읽기 전용으로 백필했다. **부족한 필드**(프롬프트 해시, model_version,
원문 응답, 지연, 종목별 토큰, 뉴스 available_at, 후보 전부, 거부 이유)는 채워 넣지 않고
비워 뒀다 — 이 필드들은 코드 동결 후 새로 쌓아야 한다.

## 게이트

| Gate | 상태 |
|---|---|
| 1 재현성 | **통과** |
| 2 실행 무결성 | **모의 통과 / 실환경 미검증** |
| 3 연구 후보 | **미확정** (C1 탈락, C2·C3 CI 가 0 포함) |
| 4 forward shadow | **미시작** (기록 기반만 준비) |
| 5 실제 배포 | **보류 유지** |

## 승인이 필요한 것 → `approval_requests.md`
A 대시보드 분리 · B 토스 토큰 정책 공식 확인 · C QQQ 실패 정책 + **N1 최신성 판정 적용**
(수정안·테스트 10건 완료, 운영 미적용).
