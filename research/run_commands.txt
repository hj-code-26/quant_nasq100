# 재현 명령 (저장소 루트에서)
# 데이터: data_cache/*.pkl (토스 일봉, 오프라인) + research/data/QQQ_1d.pkl (yfinance)
# 버전·해시·seed·구성종목 기준일·국면지표 출처는 research/out/manifest.json 에 있다.
#
# 격리: 모든 연구 스크립트는 첫 줄에서 research.isolation.guard() 를 부른다.
#   · TOSS_*/ANTHROPIC_* 환경변수 제거, .env·토큰 파일 읽기 차단(빈 내용 대체)
#   · 운영 DB(+WAL/SHM)·토큰·data_cache·운영 소스 **쓰기 차단**
#   · 토스 호스트 HTTP 차단 (yahoo 만 허용), 운영 로그는 research/out/ 로 리다이렉트
#   · 실행 전후 지문(크기·mtime·sha256) 비교

# 0) 격리 baseline 기록 (한 번)
python research/isolation.py

# 1) 기존 테스트 (수정 보존 확인)
python test_validate.py
python test_indicators.py
python test_session.py

# 2) QQQ 국면지표 확보 (무료·비상업. 유료 구매·브로커 API·신규 토큰 없음)
python research/fetch_index.py

# 3) A/B/C 원장 재현·회계 항등식·비용 민감도  → manifest.json, ab_summary.csv,
#    ab_trade_diff.csv, daily_nav.csv, cost_sensitivity.csv, accounting_identity.csv
python research/ab_audit.py

# 4) A/B 차이의 완전 분해 (현금흐름 항등식 + 거래 단위 4갈래, 반올림 전 합계 검증)
#    → ab_attribution.csv, ab_attribution.md
python research/ab_attribution.py

# 5) 안전성·엔진 무결성 회귀 (실제 함수, 22건)
python research/test_safety.py

# 6) 불변식 + 결함 주입 + 손계산 원장 (12건)
python research/test_invariance.py

# 7) 짝지은 block bootstrap + walk-forward (신뢰수준·단위·해석 주석 포함)
python research/stats.py

# 8) 기준선 + C1/C2/C3 + 국면지표 출처 추적 + C1 vs 현금혼합
#    → experiment_registry.csv, experiment_results.csv, regime_source.csv,
#      c1_vs_cashmix.csv, risk_reduction_compare.csv
python research/experiments.py

# 9) forward shadow 스키마 + replay/주입내성 (5건)
python research/shadow.py

# 10) 승인 대기 3건 모의 테스트 (9건)
python research/test_ops.py

# 11) N1 지수 최신성 판정 수정안 + 테스트 (10건, 연구 구현·운영 미적용)
python research/index_freshness.py

# 12) F 상관군 한도 + nested walk-forward (선택/평가 분리, embargo 20일, LOFO)
#     → corr_grid.csv, corr_nested.csv, corr_bootstrap.csv, corr_lofo.csv,
#       corr_cost_sensitivity.csv   결론: research/corr_conclusion.md
python research/corr_limits.py

# 13) PIT 구성종목(2015~) 재실행 — 검증 구간 편향 측정
#     → pit_bias.csv, pit_missing.csv, pit_bootstrap.csv  결론: research/pit_conclusion.md
python research/pit_universe.py --fetch    # 옛 구성종목 시세 (yfinance, 1회. 이미 받았으면 생략)
python research/pit_universe.py

# 14) 기존 코드 vs PIT 반영 코드 — 같은 데이터·같은 규칙, 스위치 하나만 차이
#     → pit_compare.csv  (불변식: 2015 이전 구간은 일별 수익률 완전 일치)
python research/pit_compare.py
PIT=0 python backtest.py --daily     # 옛 수치 재현 (편향 있는 유니버스)
python backtest.py --daily           # PIT 반영이 기본값

# 15) G — 청산·국면·진입·보조지표 사전등록 그리드 (PIT 2015~, N=32+조합1)
#     → g_ic_*.csv, g_cohort_*.csv, g_grid.csv, g_bench.csv, g_after_*.csv  결론: research/g_conclusion.md
python research/exit_study.py
#     사후 강건성 (+5 시도): 이웃·최소변경안·연도 하나 빼기·QQQ 대비 → g2_grid.csv, g2_loyo.csv, g2_vs_qqq.csv
python research/exit_robust.py
#     걷는 선택(과거 자료로만 고르기)·QQQ 혼합·QQQ 회귀 → g3_picks.csv, g3_select.csv, g3_blend.csv, g3_alpha.csv
python research/wf_select.py

# 격리 검증만 다시 보기
python -c "import sys;sys.path.insert(0,'.');from research.isolation import verify;print(verify())"

# 한 번에:
#   python research/isolation.py
#   for f in ab_audit ab_attribution test_safety test_invariance stats experiments shadow test_ops index_freshness; do python research/$f.py; done

# ── 파일 구분 (중요) ──────────────────────────────────────────────────────
# research/out/**       전부 **재생성물**. 지우고 위 명령을 다시 돌리면 그대로 복원된다.
# research/data/**      오프라인 캐시 (QQQ). fetch_index.py 가 없으면 새로 받는다.
# research/*.py         코드
# research/*.csv|md|txt 수작성 산출물 — audit_status.csv, data_gap.csv,
#                       approval_requests.md, conclusion.md, run_commands.txt.
#                       재생성되지 않으므로 out/ 을 지워도 남는다.

# --- 라이브 무거래 진단 (2026-09-16) ---
# 운영 DB 사본 + 실제 validate_orders 로 차단 단계 재현. 네트워크·LLM 호출 없음.
python research/live_funnel.py
#   → research/out/live_funnel.csv          run 별 퍼널 (판단/주문/제외/가용현금)
#   → research/out/live_funnel_replay.json  현금 수준별 차단 재현
#   보고서: research/live_diagnosis.md

# 라이브 vs 백테스트 간극 (최소주문 비율 · 하락 국면 진입 차단)
python research/live_gap.py
#   → research/out/live_gap.csv   ΔSharpe + 짝유지 블록 부트스트랩 95% CI
#   exit_study.sim 에 opt-in 2개 추가 (기본 꺼짐, check/check_prefix 통과 확인):
#     cfg["bear_noentry"]  하락 국면 신규진입 차단 (현 2단계 프롬프트 근사)
#     cfg["probe"]         진입 차단 일수 집계 dict

# 조건부 만기 청산 후보 (시간만이 조건인 문제)
python research/exit_condition.py
#   → research/out/exit_condition.csv           운영 형태 vs 백테스트 형태 + 이웃
#   → research/out/exit_condition_scorecard.csv 사전등록 채택 기준 전체 (누적 시도 N=50)
#   보고서: research/exit_condition.md
#   exit_study.sim opt-in 추가: cfg["extend_daily"] — 연장 시 보유 시계를 리셋하지 않는다(운영 형태)

# 현금을 만들려면 무엇을 얼마나 팔아야 하나 (산수 + 시스템 규칙. 투자 판단 아님)
python research/cash_raise.py
#   → research/out/cash_raise.csv
